agent_cls: skyrl_agent.agents.oh_codeact.OHCodeActAgent

task: skyrl_agent.tasks.swebench.utils.SWEBenchTask

tools:
  enable_bash_tool: true
  enable_browsing: false
  enable_llm_editor: false
  enable_editor: true
  enable_jupyter: false
  enable_cmd: true
  enable_think: false
  enable_finish: true
  enable_mcp: false
  enable_search: true

data:
  instance_key: instance
  instance_id_key: instance.instance_id
  data_source_key: data_source

generator:
  infer_backend: tinker
  backend_config: null
  num_trajectories: 8  # Same as run_mini_swe_agentforge8B.sh (n_samples_per_prompt=8)
  max_iterations: 35   # Same as run_mini_swe_agentforge8B.sh (max_turns=35)
  max_prompt_length: 4096  # Same as run_mini_swe_agentforge8B.sh
  sampling_params:
    temperature: 1.0
    top_p: 1
    max_tokens: 4096   # Same as run_mini_swe_agentforge8B.sh (max_generate_length=4096)
  val_config:
    num_trajectories: 1
    sampling_params:
      temperature: 0
      top_p: 0.95
      max_tokens: 4096
  remove_think_tokens: false
  vision_is_active: false
  qwen3_enable_thinking: false
  qwen3_acc_thinking: false

dispatcher:
  type: async_batch
  max_parallel_agents: 32
  max_eval_parallel_agents: 32
